{
  "buildCommand": "cd .. && npm install && npm run build --workspace=shared && npm run build --workspace=client",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
